#include <math.h>
#include <stdio.h>
int main() 
{
   double number, cuberoot;
   printf("Enter a number: ");
   printf("Square root of %.2lf =  %.2lf", number, squareRoot);
   cubeRoot = cbrt(number);
   printf("Cube root of %.2lf =  %.2lf", number, cubeRoot);
   return 0;
}